# CLOUD DEMO requirements (used by Streamlit Community Cloud).
# The public demo runs with DEMO_ONLY=true, which skips the heavy Kronos/torch
# model — so those libs are intentionally left out here for a fast, reliable build.
# For LOCAL development WITH the Kronos model, install requirements-local.txt instead.
streamlit>=1.35.0
yfinance>=0.2.40
plotly>=5.22.0
pandas>=3.0.0
fredapi>=0.5.2
python-dotenv>=1.0.0
anthropic>=0.28.0
requests>=2.31.0
curl_cffi>=0.7.0
tigeropen>=3.5.0
numpy
